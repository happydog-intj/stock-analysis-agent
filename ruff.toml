target-version = "py311"
line-length = 100

[lint]
select = ["E", "F", "I", "UP"]
ignore = [
    "E501",   # line too long (handled by formatter)
    "F401",   # unused import (init files)
]

[lint.per-file-ignores]
"__init__.py" = ["F401"]
"tests/*"     = ["F811"]
